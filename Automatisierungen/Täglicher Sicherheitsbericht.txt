automation:
  - id: 'daily_security_report'
    alias: "Täglicher Sicherheitsbericht"
    trigger:
      - platform: time
        at: "22:00:00"
    action:
      - service: conversation.process
        data:
          agent_id: conversation.freellm_chat
          text: >
            Erstelle einen kurzen Sicherheitsbericht für heute:
            - Haustür geöffnet: {{ states('sensor.front_door_open_count') | default('0') }} mal
            - Fenster aktuell offen: {{ states('sensor.open_windows_count') | default('0') }}
            - Letzte Bewegung außen: {{ states('sensor.outdoor_motion_last') | default('unbekannt') }}
            - Rauchmelder Status: {{ states('binary_sensor.smoke_detector') }}
            - CO-Melder Status: {{ states('binary_sensor.co_detector') }}
            
            Fasse in 3-4 Sätzen zusammen und gib Empfehlungen.
        response_variable: security_report
      - service: notify.mobile_app_phone
        data:
          title: "🔒 Täglicher Sicherheitsbericht"
          message: "{{ security_report.response.speech.plain.speech }}"